# LLM Degradation Experiments - Python Dependencies
# 
# 1. First install PyTorch with CUDA 12.1:
#    pip install torch>=2.5.1 torchvision>=0.20.1 --index-url https://download.pytorch.org/whl/cu121
# 2. Then install remaining dependencies:
#    pip install -r requirements.txt

# Core
numpy>=2.1.2
transformers>=4.50.0
sentencepiece>=0.2.0
accelerate>=1.6.0
pillow>=11.0.0
python-dotenv>=1.0.0  # For loading HF_TOKEN from .env file

# Optional training/inference helpers
# Optional: Unsloth for optimized model loading (recommended)
# Install with: pip install unsloth
# If not available, falls back to standard Transformers>=2025.4.1
huggingface-hub>=0.30.2
xformers>=0.0.29.post3

# For 4-bit quantization (REQUIRED if you use load_in_4bit=True)
# bitsandbytes>=0.45.5

# Optional analytics (uncomment if needed)
# pandas>=2.2.3
# matplotlib>=3.9.0
# seaborn>=0.13.2
# jupyter>=1.0.0
